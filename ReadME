hello, those are the instrucsions :

1. repo wixProject - the server project and database file with the data base
  at the server please change the path (in dbFormsService and dbUsersService) of the data base to the path where you will save your data base
2. repo wixProjectClient- the client project
  home page :  http://localhost:4200/Forms
  
  
